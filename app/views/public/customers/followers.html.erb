<div class="container px-5">
  <div class="row d-flex justify-content-center">
    <div class="col-8">
      <h3 class="my-5">フォロワー</h3>
      <div class="row justify-content-end mb-1">
        <%= link_to 'マイページへ', mypage_path(current_customer.id), class: "btn btn-secondary btn-sm ml-5" %>
      </div>
      <% if @customers.exists? %>
        <%= render "public/customers/index", customers: @customers %>
      <% else %>
        <p>ユーザーはいません</p>
      <% end %>
    </div>
  </div>
</div>