<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-10">
      <h2 class="my-5">フォロワー</h2>
      <% if @customers.exists? %>
        <%= render "public/customers/index", customers: @customers %>
      <% else %>
        <p>ユーザーはいません</p>
      <% end %>
      <div class="row justify-content-end mb-4">
        <%= link_to 'マイページへ', mypage_path(current_customer.id), class: "btn btn-secondary ml-5" %>
      </div>
    </div>
  </div>
</div>