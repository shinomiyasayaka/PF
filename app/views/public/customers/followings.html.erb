<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-10">
      <h2 class="my-5">フォロー</h2>
      <% if @customers.exists? %>
        <%= render "public/customers/index", customers: @customers %>
      <% else %>
        <p>ユーザーはいません</p>
      <% end %>
    </div>
  </div>
</div>